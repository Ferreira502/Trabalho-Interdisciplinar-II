function gerarRelatorio() { alert("Gerar relatório..."); }
    function selecionarRelatorioRapido(tipo) { alert("Selecionado: " + tipo); }
    function exportarPDF() { alert("Exportando PDF..."); }
    function exportarExcel() { alert("Exportando Excel..."); }
    function exportarCSV() { alert("Exportando CSV..."); }
    function enviarEmail() { alert("Enviar por e-mail..."); }
    function imprimirRelatorio() { window.print(); }
    function logout() { if(confirm("Deseja sair?")) location.href="login.html"; }